def factorial(n):
    n = int(n)
    if n < 0:
        raise ValueError("Факториал не определен для отрицательных чисел.")
    # Проверка на ноль
    elif n == 0:
        return 1
    else:
        result = 1
        for i in range(1, n + 1):
            result *= i
        return result
